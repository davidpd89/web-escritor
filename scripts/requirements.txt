# Dependencias Python no estandar usadas por scripts/tests del repositorio.
# Mantener versiones exactas: los workflows que ejecutan la bateria Python
# y el pipeline de imagenes consumen este mismo fichero para evitar entornos
# CI divergentes. Pillow 12.3.0 es la version validada para AVIF/WebP en
# Python 3.12 (L.1, 2026-08-23).
Pillow==12.3.0
